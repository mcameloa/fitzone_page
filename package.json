{
  "name": "fitzone-page",
  "version": "1.0.0",
  "description": "FitZone — Catálogo virtual TSP",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "seed": "node scripts/seed-products.mjs",
    "deploy": "firebase deploy --only hosting,firestore:rules"
  },
  "dependencies": {
    "firebase": "^12.15.0",
    "firebase-admin": "^12.1.0"
  },
  "devDependencies": {
    "firebase-tools": "^14.14.0"
  }
}
